pushd .

#
# record existing git refs in case updates cause breakage
#
find $HOME/src/grox/extern -maxdepth 2 -type d -name .git -exec git --git-dir {} log --format=format:"%H " HEAD -1 \; -print > $HOME/src/grox/scripts/gitrefs.txt


# Build Qt-Creator
# https://wiki.qt.io/Building_Qt_Creator_from_Git_on_Ubuntu_22.04

# json header only
# curl https://raw.githubusercontent.com/nlohmann/json/develop/single_include/nlohmann/json.hpp -o ./extern/nlohmann/json.hpp

# update all externs
printf "\n#---------------------------"
echo "HighFive"
cd $HOME/src/grox/extern/HighFive/
pwd && git branch && git pull

printf "\n#---------------------------"
echo "QDarkStyleSheet"
cd $HOME/src/grox/extern/QDarkStyleSheet/
pwd && git branch && git pull

printf "\n#---------------------------"
echo "Qt-Advanced-Docking-System"
cd $HOME/src/grox/extern/Qt-Advanced-Docking-System/
pwd && git branch && git fetch --all && git pull --rebase origin master

printf "\n#---------------------------"
echo "magic_enum"
cd $HOME/src/grox/extern/magic_enum/
pwd && git branch && git pull

printf "\n#---------------------------"
echo "Qwt"
cd $HOME/src/grox/extern/qwt
pwd ; git branch ; git fetch --all && git pull --rebase origin develop

printf "\n#---------------------------"
echo "pika"
cd $HOME/src/grox/extern/pika
pwd ; git branch ; git fetch --all && git pull --rebase origin main

printf "\n#---------------------------"
echo "aqtinstall"
cd $HOME/src/grox/extern/aqtinstall
pwd ; git branch ; git fetch --all && git pull --rebase origin master

printf "\n#---------------------------"
echo "qt6-docker"
cd $HOME/src/grox/extern/qt6-docker
pwd ; git branch ; git fetch --all && git pull --rebase origin master

popd

